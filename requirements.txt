# ============ Web 框架 ============
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ============ 数据校验 / 配置 ============
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9        # FastAPI 文件上传（UploadFile）依赖

# ============ 文档解析（第一阶段） ============
pdfplumber>=0.11.0             # PDF 逐页文本提取（替代已维护模式的 PyPDF2）
