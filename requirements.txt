# ============ Web 框架 ============
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ============ 数据校验 / 配置 ============
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9        # FastAPI 文件上传（UploadFile）依赖

# ============ 文档解析（第一阶段） ============
pdfplumber>=0.11.0             # PDF 逐页文本提取（替代已维护模式的 PyPDF2）

# ============ MySQL 元数据存储（第二阶段） ============
sqlalchemy>=2.0.30             # ORM
pymysql>=1.1.0                 # MySQL 驱动（纯 Python，无需编译）

# ============ Embedding / 向量库（第三阶段） ============
# 说明：sentence-transformers 会自动拉取 CPU 版 torch；
# 若需 GPU 加速，请先用官方命令按 CUDA 版本装 GPU torch（或直接用已装好 GPU torch 的 conda环境，如本项目实际的 rag_kb 环境），
# 再执行 pip install，避免被 CPU 版覆盖。
sentence-transformers>=3.0.0   # 本地 bge-m3 embedding 模型（依赖 torch）
pymilvus>=2.4.0                # Milvus 向量库 Python 客户端
